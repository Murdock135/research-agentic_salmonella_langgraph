To download the Hugging Face datasets, run the downloader module from the project root.

- Using uv (recommended):
  - `uv run -m utils.download_data`
- Using Python directly:
  - `python -m utils.download_data`
